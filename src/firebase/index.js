import { addCategory, removeCategory } from './category';
import { addNote, updateNote, removeNote } from './notes';

export {
  addNote,
  updateNote,
  removeNote,
  addCategory,
  removeCategory
};